[project]
name = "ai-models"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Amir", email = "masihiyan@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
ai-models = "ai_models:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
